# QuantRegimeTracer runs in real-data mode by default.
# Use data_mode=auto to allow explicit sample fallback when offline.
# Use data_mode=sample only for deterministic tests or reproducible local examples.

# Optional frontend override:
# VITE_API_BASE=http://localhost:8000
